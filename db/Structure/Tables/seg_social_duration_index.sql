
DROP TABLE IF EXISTS `seg_social_duration_index`;
CREATE TABLE `seg_social_duration_index` (
  `duration_nr` int(11) NOT NULL,
  `duration_index` varchar(50) NOT NULL,
  PRIMARY KEY (`duration_nr`)
);
